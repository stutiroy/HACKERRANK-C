#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int i=0;
    char s[1000];
    fgets(s, sizeof(s), stdin); 
    while(s[i]!='\0')
    {
        if(s[i]==' ')
        {
            printf("\n");
        }
        else  {
        printf("%c",s[i]);
        }
        i=i+1;
    }
    
    
    return 0;
}

